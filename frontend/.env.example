# SCAMCHECK Frontend Environment Configuration
# Optional: Set full backend API base URL if deploying frontend separately
# Leave empty or omitted in standard setup (uses relative '/api' proxy)
VITE_API_URL=
